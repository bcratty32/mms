name: "CodeQL"

on:
  push:
    branches: [ "develop", "master" ]
  pull_request:
    branches: [ "develop" ]
  schedule:
    - cron: "4 23 * * 1"

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 11

      # Initialize for built-in queries
      - name: Initialize CodeQL for built-in queries
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}
          queries: +security-and-quality

      # Initialize for CWE-502 queries
      - name: Initialize CodeQL for CWE-502 queries
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}
          queries: java/test/experimental/query-tests/security/CWE-502/

      - name: Autobuild
        uses: github/codeql-action/autobuild@v2

      # Analysis for CWE-502 queries
      - name: Perform CodeQL Analysis for CWE-502 queries
        uses: github/codeql-action/analyze@v2
        with:
          language: ${{ matrix.language }}
          output: .github/codeql/cwe502-results # Optional: Store results in a separate directory

      # Analysis for built-in queries
      - name: Perform CodeQL Analysis for built-in queries
        uses: github/codeql-action/analyze@v2
        with:
          language: ${{ matrix.language }}
